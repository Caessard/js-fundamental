var name = 'César', lastname = 'Dueñas'

var nameUppercase = name.toUpperCase()
var lastnameLowercase = lastname.toLowerCase()

var firstNameLetter = name.charAt(0)

var amountLetterInName = name.length

var fullName = name + ' ' + lastname

var fullName_new = `${name} ${lastname}`

var fullName_jsInside = `${name} ${lastname.toUpperCase()}`

var str = name.substr(1, 2)//name.charAt(1) + name.charAt(2)

var lastNameLetter = name.charAt(name.length-1)